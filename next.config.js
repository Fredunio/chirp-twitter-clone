/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true,
    },
    images: {
        domains: ['zsgilcmmxlibiomllutb.supabase.co'],
    },
}

module.exports = nextConfig
